import hoc from 'src/hocs/hoc'
const setProps=state=>setState=>
{
  const props=
  {
  }
  return props
}
const iState=
(
  ()=>
  (
    {
      show:true
    }
  )
)
()

export default hoc(iState)(setProps)
